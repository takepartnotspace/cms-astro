---
import { tinaField } from "@tinacms/astro/tina-field";
import type { CmsConfig, CmsConfigContactLink } from '../lib/data';
import IconLink from "./IconLink.astro";
import Starfield from './space/Starfield.astro';

interface Props {
	config?: CmsConfig | null;
}

const { config } = Astro.props;
const contactLinks = (config?.contactLinks ?? []).filter(
	(item): item is CmsConfigContactLink => item !== null,
);
---
<footer class="relative overflow-hidden border-t bg-background py-10 text-center text-base text-muted-foreground">
	{config?.footerStarfield !== false && <Starfield count={50} class="text-foreground/15" />}
	<div class="relative mx-auto flex max-w-6xl flex-col items-center gap-8 px-6">
		{contactLinks.length > 0 && (
			<div class="flex justify-center gap-8 scale-125 md:scale-150 py-2">
				{contactLinks.map((link) => (
					<IconLink
						title={link.title}
						link={link.link}
						icon={link.icon}
						data-tina-field={tinaField(link, "title")}
					/>
				))}
			</div>
		)}
		
		<a 
			href="/impressum" 
			class="transition-colors hover:text-foreground hover:underline"
		>
			Impressum
		</a>
	</div>
</footer>